package ucf.assignments;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class AppTest {

    @Test
    void addList() {
        /*
        - Start with an empty App.todoLists.
        - use App.addList to add a new list.
        - Check if App.todoLists is no longer empty.
         */
    }

    @Test
    void removeList() {
        /*
        - Start with a number of lists in App.todoLists.
        - Save the list at a certain index to a temp variable.
        - Use App.removeList to remove the list at that index.
        - The list at this index should no longer be equal to the one
          stored in the temp variable.
         */
    }


}