package ucf.assignments.data;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class FileHandlerTest {

    @Test
    void testReadWrite() {
        /* This tests both single and multiple file save/load.
        - Create a few lists of random items.
        - Save the lists to a file.
        - Load the lists from the file into temp variables.
        - Assert that the original lists and the saved/loaded temp variables are equal.
         */
    }
}