package ucf.assignments.todo;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class DueDateTest {

    @Test
    void testDateChange() {
        /*
        Set the year, month and day of an Item's DueDate to Year X, Month Y, and Day Z.
        Assert that the getter returns these same values.
        Set the date to Year A, Month B, and Day C.
        Assert that the getter now returns the new values.
         */
    }
}